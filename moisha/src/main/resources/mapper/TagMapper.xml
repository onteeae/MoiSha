<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.moisha.repository.TagRepository">
  <select id="selectTagsAll" resultType="tag">
    SELECT
      *
    FROM
      tag
  </select>
</mapper>